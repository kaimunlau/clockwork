# frozen_string_literal: true

source 'https://rubygems.org'

# Use Rake as a task runner
gem 'rake'

# Use SQLite as a database
gem 'sqlite3'

# Use Sequel as an ORM
gem 'sequel'

# Use RSpec as a testing framework
gem 'rspec'

# Use CLI UI as a user interface
gem 'cli-ui'
